import {StyleSheet} from "react-native";

export const ScoreViewStyleSheet = StyleSheet.create({
  view: {
    alignItems: "center",
    bottom: 0,
    flex: 1,
    justifyContent: "flex-end",
    margin: 64,
    zIndex: 100
  }
});
