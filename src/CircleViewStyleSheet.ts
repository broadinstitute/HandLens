import {StyleSheet} from "react-native";

export const CircleViewStyleSheet = StyleSheet.create({
  text: {},
  view: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "red",
    borderRadius: 64 / 2,
    height: 64,
    width: 64,
  }
});